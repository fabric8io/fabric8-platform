data:
  config.yml: |
    apiserver: ${APISERVER_HOSTPORT}
    node-ip: ${NODE_IP}
    exposer: ${EXPOSER}
    domain: ${DOMAIN}
    watch-current-namespace: true
    http: ${HTTP}
    tls-acme: ${TLS_ACME}