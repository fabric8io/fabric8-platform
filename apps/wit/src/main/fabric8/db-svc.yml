kind: Service
apiVersion: v1
metadata:
  name: db
  labels:
    service: db
spec:
  ports:
    - name: "postgres"
      protocol: TCP
      port: 5432
      targetPort: 5432
  selector:
    service: db
  type: ClusterIP